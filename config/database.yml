development:
  adapter: mysql
  encoding: utf8
  reconnect: false
  database: le_development
  pool: 5
  host: localhost
  username: root
  password: password

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: mysql
  encoding: utf8
  reconnect: false
  database: le_test
  pool: 5
  host: localhost
  username: root
  password: password

#Production username and password not included
production:
  adapter: mysql
  encoding: utf8
  reconnect: false
  database: linguistic_explorer
  pool: 5
  host: localhost
