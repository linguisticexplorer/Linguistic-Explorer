- if @group && !@group.new_record?
  - if user_signed_in?
    %li#userInfo.pull-right.dropdown.black-dropdown
      // First Line: User info
      =link_to group_memberships_path(@group, @group.membership_for(current_user)), :class => "compact" do
        %p{:style=> "margin-bottom: 0"}= current_user.name
        // - if user_is_member?
        - if current_user.admin?
          .col-md-.nav-status
            = "Site Admin"
        - elsif current_user.administrated_groups.include? @group
          .col-md-.nav-status 
            = icon 'group', 'Group Admin'
        - elsif (current_user.member_of? (@group))
          .col-md-.nav-status
            - if (current_user.is_expert? @group)
              = icon 'certificate', 'Expert'
            - else
              = icon 'user', 'Member'
      %ul.dropdown-menu{"aria-labelledby" => "dLabel", :role => "menu"}
        %li
          = link_to destroy_user_session_path do
            = icon 'sign-out', 'Sign out'
    // First Line: User info
    // - if user_has_any_role?
    //  %li.dropdown.black-dropdown
    //    =link_to group_memberships_path(@group, @group.membership_for(current_user)) do
    //      %p{:style=> "margin-bottom: 0"}= current_user.name
    //    // Second Line: User/Member Role
    //    - if current_user.admin?
    //      .col-md-.nav-status
    //        = "Site Admin"
    //    - elsif current_user.administrated_groups.include? @group
    //      .col-md-.nav-status 
    //        = icon 'group', 'Group Admin'
    //    - elsif (current_user.member_of? (@group))
    //      .col-md-.nav-status
    //        - if (current_user.is_expert? @group)
    //          = icon 'certificate', 'Expert'
    //        - else
    //          = icon 'user', 'Member'
    //- else
    //  = current_user.email
  - else
    %li.pull-right.buttons
      = link_to "Login", new_user_session_path, :class => "btn btn-default"
  / %li.pull-right.buttons
  /   = link_to "Sign Up", new_user_registration_path, :class => "btn btn-default"
