<% page_heading "Edit " + current_group.category_name %>

<h1>Editing <%= current_group.category_name %></h1>

<%= render 'form' %>

<%= link_to 'Show', [current_group, @category], :class => "btn" %> |
<%= link_to 'Back', group_categories_path(current_group) %>
