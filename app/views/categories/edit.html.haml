- page_heading "Edit " + current_group.category_name
%h1
  Editing #{current_group.category_name}
= render 'form'
= link_to 'Show', [current_group, @category], :class => 'btn btn-default'
|
= link_to 'Back', group_categories_path(current_group)}, :class => 'btn btn-default'
