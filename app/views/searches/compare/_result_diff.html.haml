- parent, children = result.parent, result.child
= content_tag :tr, search_result_attributes_for_compare(result) do
  %td= row_methods[:compare_property].call(*parent)
  - sort_by_ling(children, result.lings).each do |child|
    %td= compare_diff_value(child)
