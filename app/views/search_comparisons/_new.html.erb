<%= form_for :comparison, :url => [current_group, :search_comparisons] do |f| %>
  <%= f.label :type, "Perform" %> <%= f.select :type, @comparison.comparison_options %>
  <%= f.label :of_id, "of" %> <%= f.select :of_id, @comparison.search_options %>
  <%= f.label :with_id, "with" %> <%= f.select :with_id, @comparison.search_options %>
  <%= f.submit "Go" %>
<% end %>
