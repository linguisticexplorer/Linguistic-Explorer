$(function(){
  var newPage = "<%= escape_javascript(render("index_partial")) -%>"
  $("#pagination_table").html(newPage);
});

$( document ).ready(function() {
  $("#auto_memb").typeahead({
     source: items,
     updater:function (item){
        window.location.href = "/groups/" + group + "/memberships/" + dict[item]; 
     }
  });
});
