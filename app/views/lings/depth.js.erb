$(function(){
  var newPage = "<%= escape_javascript(render({:partial => "depth_table", :locals => {:lings => @lings, :depth => @depth} })) -%>";
  $("#pagination_table").html(newPage);
});

$( document ).ready(function() {
  $("#auto_lang").typeahead({
     source: items,
     updater:function (item){
        window.location.href = "/groups/" + group + "/lings/" + dict[item]; 
     }
  });
});
